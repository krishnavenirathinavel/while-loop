#include <stdio.h>
int main()
{
  int N;
  int i = 0;
  int risk,safe,value = 0;
  int succesed = 0,cancelled = 0,order;
  scanf("%d",&N);
  while(i  < N){
    scanf("%d",&order);
    if (order  == 1){
       succesed ++;}
       else if( order== 0){
       cancelled++;}
       i++;
    }
    printf("succesed :%d\n",succesed);
    printf("cancelled :%d\n",cancelled);
    if(succesed > cancelled){
    printf("status : risk");}
    else{
    printf(" status : safe");}
    return 0;
  }
  
  